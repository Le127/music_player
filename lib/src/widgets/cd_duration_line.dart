import 'package:flutter/material.dart';

class CdDurationLine extends StatelessWidget {
  const CdDurationLine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Row(
      children: [
        // TODO: Disco girando

        //TODO: Linea Progreso
      ],
    ));
  }
}
